#include <stdio.h>

int main()
{

    int dias, hh, mm, ss, diafinal, hhf, mmf, ssf;
    scanf("Dia %d", &dias);
    scanf("%d : %d : %d", &hh, &mm, &ss);
    scanf("Dia %d", &diafinal);
    scanf("%d : %d : %d", &hhf, &mmf, &ssf);
    return 0;
}